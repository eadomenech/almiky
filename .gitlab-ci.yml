before_script:
 - apt-get install python3-venv
 - source ~/.proxy
 - python3 -m venv venv
 - source venv/bin/activate
 - pip install -r requeriments.txt
    
stages:
 - tests
 
run-all-tests:
 stage: tests
 script:
  - source run-test.sh
 tags:
  - test
  - python
  - coverage